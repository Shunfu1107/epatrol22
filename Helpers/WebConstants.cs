namespace AdminPortalV8.Helpers
{
    public class WebConstants
    {
        public static readonly string? DateFormat = "dd-MM-yyyy";
        public static readonly string? DateTimeFormat = "dd-MM-yyyy HH:mm";

        public static readonly string? JsonDateFormat = "yyyy-MM-dd";
        public static readonly string? JsonDateTimeFormat = "yyyy-MM-dd HH:mm";
    }
}
